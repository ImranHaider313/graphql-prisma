const Comment = {
  // author(parent, args, { prisma }, info){
  //   return prisma.users.find(user => user.id === parent.author)
  // },
  // post(parent, args, { prisma }, info){
  //   return prisma.posts.find(post => post.id === parent.post)
  // }
}

export { Comment as default }